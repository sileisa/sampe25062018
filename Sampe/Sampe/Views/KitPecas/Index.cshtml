@model IEnumerable<Sampe.Models.KitPeca>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OrdemProducaoKit.CodigoIdentificadorKit)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OrdemProducaoPeca.CodigoIdentificador)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OrdemProducaoKit.CodigoIdentificadorKit)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OrdemProducaoPeca.CodigoIdentificador)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.KitPecaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.KitPecaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.KitPecaId })
        </td>
    </tr>
}

</table>
