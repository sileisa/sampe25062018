@model IEnumerable<Sampe.Models.Venda>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Relatorio/LayoutRelatorio.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EspecificacaoCopo.LoteMaster)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ExpedicaoCopo.ExpedicaoId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ValorUnitario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Subtotal)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EspecificacaoCopo.LoteMaster)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ExpedicaoCopo.ExpedicaoId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ValorUnitario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Subtotal)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VendaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.VendaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VendaId })
        </td>
    </tr>
}

</table>
